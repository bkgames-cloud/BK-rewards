Dépose ici les assets de branding v1 :

- `icon_v1.png` : icône app (idéalement carré, haute résolution).
- `splash_v1.png` : splash screen (format portrait recommandé).

Ces fichiers sont référencés par `public/manifest.json` (PWA) et peuvent aussi servir de source pour générer les assets Android (Capacitor).

